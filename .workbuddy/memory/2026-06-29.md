## 2026-06-29 省份筛选 bug 修复

**问题：** 看板省份下拉筛选无响应
**根因：** `applyFilters()` 首行调用 `updateProvinceDropdown()`，后者 `innerHTML=''` 重置了下拉框 DOM，导致已选中的省份值在重建后丢失，读到的 `province` 始终为空
**修复：** 在 `updateProvinceDropdown()` 重建选项前保存 `provSel.value`，重建后若原值仍在列表中则恢复；切换区域时省份自动重置（原值不在新列表中）
**文件：** `iv_dashboard.html` + 桌面备份同步更新

## 2026-06-29 舆情×销量错配 Marimekko 比例矩阵

**需求：** 把九宫格升级为按26年销量体量比例绘制的矩阵，涨跌用绿/红饱和度区分，每格内净增长/净流失用最高饱和度色块，左上角家数徽标悬浮显示该象限Top10
**实现：**
- `/tmp/gen_grid_data.py` → 生成 `/tmp/grid_data.json`（4销量趋势×3舆情趋势=12格，每格 count/s25/s26/growth/loss/rate/magnitude/top10）
- `/tmp/grid_template.html` 模板 + 注入 → `iv_mismatch_matrix.html`（18.7KB，独立可打开）
- 布局：Marimekko，列宽∝舆情列体量，格高∝该格体量，面积=业务规模；增长格高饱和绿块=净增额，下降格高饱和红块=净流失额，业务丢失=深红#7f1d1d，平稳=灰
- 徽标 hover → fixed tooltip 显示 Top10（医院名/省份/s25→s26/变动额）
**关键结论：** 舆情×销量相关系数仅0.085；错配227家(56%)。A逆境韧性49家(舆情恶化却增长)、B无声流失178家(舆情好却跌)、C预期受损90家、D顺势24家
**分析框架：** 合伙人视角 = 先证明错配是常态→MECE四象限→A象限提炼抗舆情样板/B象限叠加竞品人员政策数据做根因
**产物：** `iv_mismatch_matrix.html`, `iv_mismatch_detail.csv`

## 2026-06-29 矩阵改造v2 + 嵌入看板 + 切片对比模块

**改进点(用户反馈)：**
1. 加回坐标轴：Y轴"销量趋势"(竖排) + X轴"舆情趋势（改善→稳定→恶化）"
2. 改掉Marimekko拼接 → 4×3等大象限盒(132px)，盒内色块面积∝业务体量(25/26较大值/全局最大格)，sqrt映射边长，靠左上对齐
3. 矩阵嵌入主看板(原独立iv_mismatch_matrix.html逻辑移植)，随筛选器联动；用看板自带sales_trend/sentiment_trend字段+sumHalf(AP1-6同期)计算
4. 新增2个纵向切片对比框：维度=区域/省份/季度(24Q1..26Q4)，每框最多选3切片；区域/省份→月度多折线对比，季度→柱状对比

**实现细节：**
- 矩阵JS函数：buildMatrixCells/mxBaseFill/renderMatrix/mxShowTip(Top10 tooltip)
- 对比JS：CMP_STATE/cmpSliceOptions/onCmpDimChange/toggleCmpSlice/cmpSeriesForSlice/renderCompare/initCompare
- 全局最大格=下降×稳定(3639万,边长1.0)；季度划分 quarter=ceil(AP/3)
- bootstrap加 initCompare()；applyFilters()末尾加 renderMatrix/renderCompare(0)/renderCompare(1)
- 校验：node vm语法通过，矩阵数值模拟正确

**产物：** iv_dashboard.html(嵌入完成) + 桌面备份同步；独立 iv_mismatch_matrix.html 保留

## 2026-06-29 对比框改为同款矩阵(v3)

**需求变更：** 对比框A/B不再是折线/柱状图，而是生成与主矩阵**同结构的矩阵**，按不同切片对比；A和B都支持全部维度(区域/省份/季度)
**重构：**
- 抽通用 `buildCellsGeneric(pool, fnA, fnB)`(基线/当前金额函数可插拔) + `renderMatrixGrid(gridId, cells, gmax, cacheKey)`
- 主矩阵 renderMatrix() 用 25→26 同期；记 window.__mainGmax 供对比矩阵**共享面积刻度**(块大小绝对可比)
- 对比矩阵 buildSliceCells(dim,val)：区域/省份→该切片医院 25→26；季度→全局当前季度 vs 去年同季(prevYearQuarter)
- 每框两个下拉：维度选择 + 切片选择(单选)；meta显示家数/体量/同比%
- tooltip 用 data-cache 区分 main/cmp0/cmp1 三套数据
- CSS：cmp-panels 改 grid 2列(窄屏1列)，cmp-grid 盒高96px
**移除旧函数：** buildMatrixCells/toggleCmpSlice/renderCmpSlices/cmpSeriesForSlice/CMP_CHARTS
**校验：** node vm语法通过；模拟 华东(167家/26体量4666万)、季度26Q2vs25Q2(-13%) 数值正确

## 2026-06-29 对比框改为同款矩阵(v3)

**需求变更：** 对比框A/B不再是折线/柱状图，而是生成与主矩阵**同结构的矩阵**，按不同切片对比；A和B都支持全部维度(区域/省份/季度)
**重构：**
- 抽通用 `buildCellsGeneric(pool, fnA, fnB)`(基线/当前金额函数可插拔) + `renderMatrixGrid(gridId, cells, gmax, cacheKey)`
- 主矩阵 renderMatrix() 用 25→26 同期；记 window.__mainGmax 供对比矩阵**共享面积刻度**(块大小绝对可比)
- 对比矩阵 buildSliceCells(dim,val)：区域/省份→该切片医院 25→26；季度→全局当前季度 vs 去年同季(prevYearQuarter)
- 每框两个下拉：维度选择 + 切片选择(单选)；meta显示家数/体量/同比%
- tooltip 用 data-cache 区分 main/cmp0/cmp1 三套数据
- CSS：cmp-panels 改 grid 2列(窄屏1列)，cmp-grid 盒高96px
**移除旧函数：** buildMatrixCells/toggleCmpSlice/renderCmpSlices/cmpSeriesForSlice/CMP_CHARTS
**校验：** node vm语法通过；模拟 华东(167家/26体量4666万)、季度26Q2vs25Q2(-13%) 数值正确

## 2026-06-29 明细列表 医院→HP + VF占比字段

**需求：** UI文字"医院"全文替换为"HP"(RAW真实医院名字符串除外)；明细列表新增"VF在分子式中的占比"字段，来源 /Users/fy/Downloads/VF_IV潜力1.xlsx (Sheet1, ACCOUNT_NAME ↔ VF在分子式中的占比)
**实现：** /tmp/vf_inject.py —— 解析RAW JSON给每家HP注入vf字段(精确名匹配)；用占位符保护RAW行后全局 replace('医院','HP')再还原；明细表头name列后插VF列(右对齐,值*100显示%,缺失显"—")；sortTable对vf=null按-1排序
**匹配率：** VF表455家 ∩ 看板408家 = 精确287家(70%)，归一化无提升(其余121家VF表确无对应名)，未匹配显"—"
**校验：** node vm语法通过；287/408有VF值；6处UI文字替换确认；桌面备份已同步

## 2026-06-29 IV Analyzer 独立软件构建

**需求确认：**
1. 上传标准格式数据表格 → 提供模板下载
2. 本地浏览分析报告（与样本 iv_dashboard.html 效果一致）
3. 脱敏功能（导出后隐藏 HP 名称，仅保留编号）
4. 严格本地运行，有网络也不联网
5. 删除样本报告多余注释文字
6. 最终交付：macOS .dmg 安装包

**架构：**
- `iv_analyzer/` 项目目录
- `src/loader.py` — Excel读取+VF匹配+模板生成
- `src/cleaner.py` — 活跃窗口检测+剔除+插值+数据构建
- `src/classifier.py` — Rev趋势5类+舆情3类+面板固定效应回归
- `src/renderer.py` — HTML注入+CSV导出+脱敏模式
- `build_report.py` — Tkinter GUI 入口
- `template_before.html` / `template_after.html` — 从看板拆分的模板
- `build_pkg.sh` — PyInstaller 打包脚本

**测试结果：** 563家 → 清洗419家 → 有效396家 → HTML(309KB)+CSV(1.4MB) → JS语法通过

## 2026-06-29 IV Analyzer macOS DMG 打包完成

**流程：**
1. 修复 spec 文件 PROJECT_DIR 双层 dirname bug → PyInstaller 6.21 成功构建
2. `.app` bundle 生成：`dist/IV Analyzer.app`（arm64，25MB）
3. DMG 封装：空 sparseimage → 挂载到项目内 mnt/ → 拷贝 .app + Applications 软链 → 卸载 → hdiutil convert UDZO（压缩率74.8%）
4. 产物：`dist/IV_Analyzer_v2.0.dmg`（25MB）已复制到桌面

**注意：** hdiutil -srcfolder 模式需要 /Volumes 挂载权限，在沙箱环境受限；改用两步法（create sparseimage → attach -mountpoint 项目目录 → detach → convert）可绕过限制

## 2026-06-29 产品更名 + 演示数据生成

**更名：** iv_analyzer → review-assistance
- 项目目录重命名
- `build_report.py`：类名 IVAnalyzerApp→ReviewAssistanceApp，窗口标题/标签/输出文件名更新
- `build_pkg.sh`：APP_NAME/DMG_NAME/使用说明/spec 全部更新
- `build/iv_analyzer.spec`：name/bundle_identifier/info_plist 更新为 Review Assistance v1.0
- `template_before.html`：title/header/footer 文字更换
- 输出命名：Review_Assistance_看板.html / .csv / 数据模板.xlsx

**演示数据生成：**
- 从 iv_dashboard.html RAW JSON 提取 408 家 HP 完整数据（35 个月）
- `demo_data.xlsx`：Sheet1 "IV-限控tracking" + Sheet2 "IV-THHtracking"，38 列（HP Code/Name/Province+35月）
- `demo_vf.xlsx`：Sheet1 VF 占比（ACCOUNT_NAME + VF在分子式中的占比）
- 流水线验证：408家→0剔除→408分类→回归 β_type1=-9.5%
- 185/408 舆情趋势与原报告一致（其余因原报告使用不同分类逻辑）

## 2026-06-29 DashboardCat 更名 + 脱敏修复 + 重新打包

**6项需求全部完成：**
1. **全局更名 DashboardCat** — build_report.py(标题×2/文件名×2)、template_before.html(title)、iv_analyzer.spec(exe+app+bundle_identifier)、footer "DashboardCat" 确认
2. **西高地白梗图标** — PIL 绘制卡通 Westie(蓝圆底+白狗)，iconset 全尺寸(16~1024+@2x)，iconutil 转换 → `build/westie.icns`(220KB)，已嵌入 app Resources + Info.plist CFBundleIconFile
3. **浏览器打开报告** — `renderer.py` 中 `open_in_browser()` 使用 `webbrowser.open('file://'+path)`，`build_report.py` 第203-204行已调用，确认无需修改
4. **脱敏模式修复** — 根因：`build_report.py` 第149行 `pivot_to_long()` 调用未传 `hospital_info`，导致 province/region 全空 → 修复为 `pivot_to_long(sent_df, sales_df, months, hospital_info)`；`gen_template_excel()` 加入 Province 列
5. **Demo 数据格式** — demo_data.xlsx 408家HP，含 Province 列，24AP1~26AP6 共35月；demo_vf.xlsx 455条 VF 占比
6. **Debug 流水线** — 408家→province/region 100%非空→正常HTML+脱敏HTML+CSV 全部通过

**打包交付：**
- PyInstaller onefile + .app bundle（arm64，29MB）
- DMG 两步法：sparseimage → attach → cp app → detach → convert UDZO（压缩率72.3%，29MB）
- 桌面交付：DashboardCat.dmg + demo_data.xlsx + demo_vf.xlsx
