# 2026-07-06 Work Log

## Unit 2 (Cell Biology) — Complete 5-part study hub built
- **Source:** `/Users/fy/Downloads/生物unit2.pdf` (22 pages, scanned, no text layer)
- **OCR pipeline:** PyMuPDF @200dpi → easyocr (CPU) → `/tmp/unit2_ocr.txt`. Extracted all glossary (P19-22) + key-words (P6/9/12/16) + reading passages.
- **Figures:** 11 cropped via OCR bbox gap analysis → `diagrams/u2_fig01-11.png` (microscope, organisation hierarchy, membrane/diffusion, osmosis, plant/animal cell osmosis, active transport, phagocytosis, exocytosis).

### unit2.html — 5-part architecture (1865 → ~2200 lines)
1. **Part 1 Vocab cards** — 46 terms (IPA + CN + EN def + excerpt + figure). Categories: structure(15) / organisation(8) / transport(6) / effect(10) / method(7).
2. **Part 2 Matching** — 46 defs → 4-option MCQ, auto-grade, reshuffle, wrong→mistake book.
3. **Part 3 CN→EN recall** — 46 items, partial scoring (exact 100 / partial 60).
4. **Part 4 Cornell notes** — 4 sections (2.1 cells / 2.2 organisation / 2.3 diffusion / 2.4 osmosis+active transport). Cue column + main reading + EN/CN summary + highlight-to-mistake-book.
5. **Part 5 Cloze + Short** — 24 cloze (fill-in) + 6 short-answer (keyword-scored, model answer EN+CN, language notes).
6. **Mistake book** — localStorage `unit2_mistakebook_v1`, tracks count/reasons/keys, PDF export (Cornell A4 9.5pt).

### Bug fixes during build
- Cornell cue renderer expected `{keywords,explain_cn,questions}` but data was `{term:explanation}` map → fixed renderer to iterate key-value pairs.
- Cloze renderer expected `text_before/text_after/hint_en` but data used `before/after/hint` → fixed renderer.
- VOCAB used `cat` field but renderers read `category` → aligned to `cat`, added CSS `cat-organisation`.
- Matching pickQuestions used `item.category` → changed to `item.cat || item.category`.

### index.html updated
- Unit 2 card: Coming Soon → Available, links to unit2.html, description updated.

### Validation
- All data pools load (46/46/46/4/24/6), 0 runtime errors, HTTP 200 on unit2.html + index.html + diagrams.

## SOP bumped to v1.5
- Archived v1.4 to `archive/SOP_v1.4_2026-07-06.md`.
- v1.5 documents: 5-part architecture, Edge-TTS setup, voice selector, tts_server.py + start_tts.sh.

## TTS engine upgraded (v1.4 → v1.5)
- **Replaced**: Google Translate TTS (low quality, frequent CORS blocks) with **Edge-TTS** (Microsoft neural network, same engine as Azure Cognitive Services / Edge browser Read Aloud).
- **Setup**: `tts_server.py` (aiohttp + edge-tts library, port 8766) + `start_tts.sh` launcher.
- **Voices**: jenny (en-US-JennyNeural default) · aria (en-US-AriaNeural crisp) · emma (en-GB-EmmaNeural UK) · ana (en-US-AnaNeural younger) · guy (male).
- **Features**: free, no API key, no rate limit, cached to `~/.cache/icmyp_tts/` (md5 hash), rate=-5% (5% slower for vocab clarity).
- **HTML integration**: probe `/health` on page load → if up, send all 🔊 requests to proxy. Failure → auto-fall-back to StreamElements → Web Speech. `TTS_PROXY_ONLINE` flag prevents retries. Voice selector dropdown in unit2 nav bar; saved to localStorage.
- unit1.html retrofitted to same engine (no voice selector — defaults to Jenny).
- Both `speakGoogleTTS` removed; old "race pattern" (Web Speech + cloud race) removed.

## Pending items
- DeepSeek API integration for AI mistake-book analysis (deferred — will batch with other AI modules later)
- GitHub Pages deployment of Unit 2 (user to confirm)
